name: construct_sce_objects
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - r-base=4.1.3
  - bioconductor-dropletutils=1.14.2
  - r-tidyverse=1.3.2
  
# set channel priority to strict with conda config --set channel_priority strict
# use --conda-frontend conda in snakemake command
